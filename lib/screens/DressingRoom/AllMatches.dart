import 'package:flutter/material.dart';

class AllMatches extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("List of all matches"),
    );
  }
}
